sap.ui.define(['sap/uxap/BlockBase'],
	function (BlockBase) {
		"use strict";

		var BlockPhoneNumber = BlockBase.extend("BuildingPermits.view.SharedBlocks.personal.BlockPhoneNumber", {
			metadata: {}
		});


		return BlockPhoneNumber;

	});
